# Pro-fit 本地篮球训练版本

## 启动与体验

```sh
npm install
VITE_DATA_MODE=local npm run dev -- --port 5173
```

打开 http://127.0.0.1:5173 。以上命令明确启用本地模式，不调用 Supabase，也不加载远程字体。省略覆盖变量时，以 `.env` 的配置为准。

登录页可以直接点击 **Coach Ben · Coach** 或 **Michael · Coachee**。手动登录资料：

| 角色 | 邮箱 | 本地演示密码 |
| --- | --- | --- |
| Coach | demo-coach@pro-fit.app | ProFit2026! |
| Coachee | demo-athlete@pro-fit.app | ProFit2026! |

这是本地演示账号，勿用于线上服务。注册页支持选择 Coach / Coachee，两者自动建立内部指导关系，无邀请码。新队员没有默认训练历史；Coach 可为其发布计划。Michael 的首次初始化包含当前周示例计划。跨周后，Coach 可通过“创建下一周”继续安排。

## 本轮功能

- 展示称呼统一为 Coach / Coachee · 队员；数据库接口仍使用原 `trainee` 标识，避免提前迁移。
- 7 日周视图、日期切换、历史反馈查看；未来日期只可预览。
- 篮球周模板：每日 45 分钟 Zone 2；周一推、周三腿与核心、周五拉；周二与周六各一节 75 分钟篮球课；周四与周日不安排力量或篮球。
- Coach 可以调整日期、动作、组数、次数、重量、时长和提示；草稿可重新打开；发布后队员可见。
- 相同日期不重复发布；已有训练记录的计划保留，避免修改历史动作标识。
- 恢复打卡：睡眠、疲劳、肌肉酸痛程度与位置、疼痛程度与位置、主观感受、有氧实际分钟与用力程度。先保存恢复记录再提交当天打卡。
- 教练可以查看队员恢复信息、照片、训练记录并反馈；反馈后状态变为 reviewed。提交后记录只读，休息日不要求完成训练。
- Zone 2 时长允许减量或为 0。提示依据身体反馈调整，不生成诊断或一刀切心率数值。

模板是学生项目的可调整示例，不是所有人的统一训练处方。45 分钟 × 7 天再加力量与篮球有较高总负荷；应结合训练经验、年龄、赛事与恢复调整。应用内有适应期、减量、疼痛处理、动作余力与青少年指导说明，以及参考原则来源。

## 数据与边界

- 账号（加盐 PBKDF2 摘要）、资料、内部关系、计划、恢复记录、打卡和反馈：`localStorage` 的 `pro-fit.local.v1`。
- 当前登录账号：`pro-fit.session.v1`。
- 照片：IndexedDB `pro-fit-local-photos`，通过临时 Blob URL 展示。
- 刷新或重启开发服务器不会清除数据；浏览器清除站点数据会删除本地记录。
- 每个浏览器 / 域名各自保存；`localhost` 和 `127.0.0.1` 不共享存储。这个版本不会跨设备同步。
- 本地角色检查只用于流程演示，不是服务器权限隔离；不要把此模式作为多人线上服务发布。
- 本地模式不会发邮箱验证或密码重置邮件。

## 后续 Supabase 接入

本轮没有创建云项目、修改远程数据或执行数据库迁移。`VITE_DATA_MODE=local` 是默认值。旧连接代码保留，但新版恢复数据、内部注册规则与训练分类尚未接入远程 schema；不要直接把环境变量改成 `supabase` 当作新版上线。

本地确认完成后，再建立新的 Supabase 项目，并迁移 / 实现：

1. Coach / Coachee 注册与内部可见范围的服务器规则；本地自动建立关系逻辑不能直接当作线上授权。
2. 恢复字段与范围校验；训练 `training_kind`、时长和日期约束。
3. 事务性的计划保存 / 发布、打卡提交和反馈状态更新。
4. RLS 与私有照片 Storage，跨账号隔离和真实双角色端到端验收。
5. 正式环境禁用本地演示账号，按需导入经确认的测试计划。

## 验证

```sh
npm test
npm run build
```

`tests/local-workflow.test.js` 覆盖模板频次与时长、恢复数值校验、注册持久化、草稿可见性、发布、打卡、教练反馈、只读保护、未来日期、重复计划和账号数据隔离。原有路由与服务测试保留。

2026-09-20 修复：统一演示账号并迁移旧 ID；恢复恢复打卡与 My Day；接通计划历史和标签页路由；统计不再使用占位值。当前验收结果以本轮测试输出为准。
