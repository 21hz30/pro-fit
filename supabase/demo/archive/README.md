# Historical scripts

These files preserve earlier experiments as plain text, not runnable setup instructions. Several used nonexistent columns, empty descending loops, or overly broad deletes. Do not execute them. Use ../seed.sql and ../check.sql for the supported demo workflow. No cleanup is needed or performed by the supported seed.
